# =====================================
# OpenEMR EKS CI/CD Test Suite Workflow
# =====================================
# This workflow provides comprehensive testing and validation for the OpenEMR EKS deployment
# across multiple dimensions including code quality, Kubernetes manifests, script validation,
# and documentation. It runs automatically on pushes and pull requests, and can be manually
# triggered with specific test suite selection.
#
# Key Features:
# - Multi-dimensional testing (code quality, K8s manifests, scripts, documentation)
# - Matrix strategy for parallel test execution across different test suites
# - Comprehensive environment setup (Python, Terraform, kubectl, shellcheck)
# - Security scanning with Trivy vulnerability scanner
# - Artifact collection and test result reporting
# - Manual workflow dispatch with test suite selection
#
# Test Categories:
# 1. Code Quality: Shell script syntax validation, common issue detection
# 2. Kubernetes Manifests: YAML syntax validation, K8s best practices
# 3. Script Validation: Shell script analysis with shellcheck
# 4. Documentation: Markdown validation and structure checks
# 5. Security Scan: Vulnerability scanning with Trivy (secrets, config, vulns)

name: CI/CD Tests

# Workflow triggers - runs on code changes and manual dispatch
# checkov:skip=CKV_GHA_7: The constrained choice selects one fixed test suite and cannot alter build commands or artifacts.
on:
  # Automatic triggers on main and develop branches
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  # Manual trigger with test suite selection
  workflow_dispatch:
    inputs:
      test_suite:
        description: 'Test suite to run'
        required: false
        default: 'all'
        type: choice
        options:
        - all
        - code_quality
        - kubernetes_manifests
        - script_validation
        - documentation
        - warp
        - credential_rotation
        - openemr_dr
        - knowledge_mcp
        - floci
        - floci_e2e_lite

# Cancel in-progress runs when a new commit is pushed to the same branch/PR
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

# Required permissions for the workflow
permissions:
  contents: read         # Read repository contents for testing
  security-events: write # Write security scan results to GitHub Security tab
  actions: read          # Read workflow information for reporting

# Environment variables for consistent tool versions across all jobs
env:
  PYTHON_VERSION: '3.14.7'    # Python version for YAML validation and dependencies
  TERRAFORM_VERSION: '1.15.8' # Terraform version for infrastructure validation
  KUBECTL_VERSION: 'v1.36.3'  # kubectl version for Kubernetes manifest validation
  UV_VERSION: '0.11.26'       # uv version for the knowledge MCP package
  TRIVY_VERSION: '0.72.0'     # Trivy engine version (synced with versions.yaml)

# Checkout/setup v7 actions require Node 24; the GitHub-hosted runners below provide it.
# Push/PR runs every job. workflow_dispatch may select a single suite.
jobs:
  # Validate all Python requirement pins against versions.yaml
  python-requirements-validate:
    name: Python Requirements Validation
    runs-on: ubuntu-26.04
    if: |
      github.event_name != 'workflow_dispatch' ||
      github.event.inputs.test_suite == 'all' ||
      github.event.inputs.test_suite == 'openemr_dr' ||
      github.event.inputs.test_suite == 'warp' ||
      github.event.inputs.test_suite == 'credential_rotation'

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install yq
        run: |
          chmod +x scripts/ci/install-yq.sh
          ./scripts/ci/install-yq.sh

      - name: Validate all Python requirement pins
        run: ./scripts/validate-python-requirements.sh all

  # Primary test job - runs test suites in parallel using matrix strategy
  # This job executes the comprehensive test suite across different categories
  # to validate code quality, Kubernetes manifests, script syntax, and documentation
  test:
    name: Run Test Suite
    runs-on: ubuntu-26.04
    # Matrix strategy for parallel execution of different test suites
    # Each test suite runs independently, allowing for faster overall execution
    strategy:
      matrix:
        # Test suite categories to execute in parallel
        test_suite: [code_quality, kubernetes_manifests, script_validation, documentation]
        include:
          # Human-readable display names for each test suite category
          - test_suite: code_quality
            display_name: Code Quality Tests
          - test_suite: kubernetes_manifests
            display_name: Kubernetes Manifest Tests
          - test_suite: script_validation
            display_name: Script Validation Tests
          - test_suite: documentation
            display_name: Documentation Tests

    steps:
      # Step 1: Checkout repository code for testing
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Step 2: Set up Python environment for YAML validation and dependencies
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # Step 3: Install Python dependencies required for testing
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml  # Required for YAML syntax validation

      # Step 4: Set up Terraform for infrastructure validation
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      # Step 5: Set up kubectl for Kubernetes manifest validation
      - name: Set up kubectl
        uses: azure/setup-kubectl@829323503d1be3d00ca8346e5391ca0b07a9ab0d # v5.1.0
        with:
          version: ${{ env.KUBECTL_VERSION }}

      # Step 6: Install shellcheck for shell script validation
      - name: Set up shellcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      # Step 6b: Install BATS (Bash Automated Testing System) for script behavior tests
      - name: Set up BATS
        run: |
          sudo apt-get install -y bats
          bats --version

      # Step 6c: Install yq for Python requirement pin validation in script_validation suite
      - name: Install yq
        run: |
          chmod +x scripts/ci/install-yq.sh
          ./scripts/ci/install-yq.sh

      # Step 7: Validate environment and dependencies before testing
      # This step ensures all required tools are properly installed and accessible
      - name: Check environment and dependencies
        run: |
          echo "=== Environment Check ==="
          echo "Current directory: $(pwd)"
          echo "Python version: $(python3 --version)"
          echo "Terraform version: $(terraform --version)"
          echo "Kubectl version: $(kubectl version --client)"
          echo "Shellcheck version: $(shellcheck --version)"
          echo "Available shells:"
          ls -la /bin/bash /bin/sh /bin/dash 2>/dev/null || echo "Some shells not found"
          echo "Script directory contents:"
          ls -la scripts/
          echo "Test config file:"
          cat scripts/test-config.yaml
          echo "=== File Check ==="
          echo "Kubernetes manifests:"
          ls -la k8s/*.yaml 2>/dev/null || echo "No k8s yaml files found"
          echo "Shell scripts:"
          ls -la scripts/*.sh 2>/dev/null || echo "No shell scripts found"
          echo "Terraform files:"
          ls -la terraform/*.tf 2>/dev/null || echo "No terraform files found"

      # Step 8: Execute the specific test suite based on matrix configuration
      # This step runs the comprehensive test suite with verbose output and debug mode
      - name: Run ${{ matrix.display_name }}
        run: |
          cd scripts
          chmod +x run-test-suite.sh
          echo "Current directory: $(pwd)"
          echo "Script contents:"
          head -20 run-test-suite.sh
          echo "Script permissions:"
          ls -la run-test-suite.sh
          echo "Running test suite: ${{ matrix.test_suite }}"
          echo "=== Starting Test Execution ==="
          set -x  # Enable debug mode for detailed output
          ./run-test-suite.sh -s ${{ matrix.test_suite }} -v
          echo "=== Test Execution Complete ==="

      # Step 9: Upload test results as artifacts for analysis and reporting
      # Results are retained for 7 days and uploaded regardless of test outcome
      - name: Upload test results
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()  # Upload artifacts even if tests fail
        with:
          name: test-results-${{ matrix.test_suite }}
          path: test-results/
          retention-days: 7

  # Lint and validation job - performs static analysis and syntax validation
  # This job runs after the test job and focuses on code quality, syntax validation,
  # and best practices enforcement across different file types
  lint-and-validate:
    name: Lint and Validate
    runs-on: ubuntu-26.04

    steps:
      # Step 1: Checkout repository code for validation
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Step 2: Set up Python environment for YAML validation
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # Step 3: Install Python dependencies for validation
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml  # Required for YAML syntax validation

      # Step 4: Set up Terraform for infrastructure validation
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      # Step 5: Set up kubectl for Kubernetes manifest validation
      - name: Set up kubectl
        uses: azure/setup-kubectl@829323503d1be3d00ca8346e5391ca0b07a9ab0d # v5.1.0
        with:
          version: ${{ env.KUBECTL_VERSION }}

      # Step 6: Validate Terraform configuration syntax and structure
      - name: Validate Terraform configuration
        run: |
          cd terraform
          terraform init -backend=false -lockfile=readonly # Validate the reviewed provider set
          terraform validate            # Validate Terraform configuration syntax

      # Step 7: Validate Kubernetes manifest YAML syntax
      # This step ensures all K8s manifests have valid YAML syntax before deployment
      - name: Validate Kubernetes manifests (YAML syntax only)
        run: |
          for file in k8s/*.yaml; do
            echo "Validating YAML syntax for $file..."
            python3 -c "import yaml; list(yaml.safe_load_all(open('$file')))"
            echo "✓ $file YAML syntax is valid"
          done

      # Step 8: Strict shellcheck validation — all findings (error, warning, info, style) fail the build
      - name: Strict shellcheck on all shell scripts
        run: |
          echo "Running strict shellcheck on all shell scripts..."
          failed=0
          for file in scripts/*.sh monitoring/*.sh k8s/*.sh oidc_provider/scripts/*.sh warp/benchmark-data/*.sh; do
            if [ -f "$file" ]; then
              echo "Checking $file..."
              if ! shellcheck "$file"; then
                failed=1
              fi
            fi
          done
          if [ "$failed" -ne 0 ]; then
            echo "::error::shellcheck found issues — all warnings must be resolved"
            exit 1
          fi
          echo "All shell scripts pass strict shellcheck"

      # Step 10: Validate YAML files across multiple directories
      # This step validates YAML syntax for Kubernetes and monitoring configurations
      - name: Validate YAML files
        run: |
          for file in k8s/*.yaml monitoring/*.yaml; do
            if [ -f "$file" ]; then
              echo "Validating YAML syntax for $file..."
              python3 -c "import yaml; list(yaml.safe_load_all(open('$file')))"
              echo "✓ $file YAML syntax is valid"
            fi
          done

      - name: Markdown lint
        run: |
          npm install -g markdownlint-cli
          markdownlint '**/*.md' --config .markdownlint.json

      - name: YAML lint
        run: |
          pip install yamllint
          yamllint -c .yamllint .

  # Security scanning job - performs comprehensive security analysis with ZERO-TOLERANCE policy
  # This job runs Trivy vulnerability scanner to detect security issues, secrets,
  # and misconfigurations across the entire repository. ALL findings will fail the build.
  security-scan:
    name: Security Scan (Zero-Tolerance)
    runs-on: ubuntu-26.04

    steps:
      # Step 1: Checkout repository code for security scanning
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Step 2: Run Trivy vulnerability scanner in table format (STRICT MODE)
      # This scan produces human-readable output and FAILS on ANY finding
      - name: Run Trivy vulnerability scanner (Table format - STRICT)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          version: 'v${{ env.TRIVY_VERSION }}'
          scan-type: 'fs'                      # File system scan of entire repository
          scan-ref: '.'                        # Scan current directory
          format: 'table'                      # Use table format for better readability in logs
          output: 'trivy-results.txt'          # Output file for table format results
          severity: 'CRITICAL,HIGH,MEDIUM,LOW' # Include all severities - zero tolerance
          scanners: 'vuln,secret,misconfig'    # Enable vulnerability, secret, and misconfig scanners
          exit-code: '1'                       # FAIL the build on ANY finding (zero-tolerance)
          cache-dir: '.trivycache'             # Use local cache directory
          trivyignores: '.trivyignore'         # Use ignore file for false positives only

      # Step 3: Run Trivy vulnerability scanner in SARIF format for GitHub Security tab
      # This runs only if table scan passes (no findings)
      - name: Run Trivy vulnerability scanner (SARIF format)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        if: success()
        with:
          version: 'v${{ env.TRIVY_VERSION }}'
          scan-type: 'fs'                      # File system scan of entire repository
          scan-ref: '.'                        # Scan current directory
          format: 'sarif'                      # SARIF format for GitHub Security tab
          output: 'trivy-results.sarif'        # Output file for SARIF format results
          severity: 'CRITICAL,HIGH,MEDIUM,LOW' # Include all severities
          scanners: 'vuln,secret,misconfig'    # Enable vulnerability, secret, and misconfig scanners
          exit-code: '1'                       # FAIL on any finding
          trivyignores: '.trivyignore'         # Use ignore file for false positives only

      # Step 4: Display Trivy scan results in workflow logs
      - name: Display Trivy results
        if: always()
        run: |
          echo "🔍 Trivy Security Scan Results (ZERO-TOLERANCE MODE):"
          echo "======================================================="
          if [ -f "trivy-results.txt" ]; then
            cat trivy-results.txt
          else
            echo "✅ No vulnerabilities found - scan passed"
          fi
          echo "======================================================="

          echo ""
          echo "📊 Scan Configuration:"
          echo "- Policy: ZERO-TOLERANCE (fail on any finding)"
          echo "- Scanners enabled: Vulnerability, Secret, Configuration"
          echo "- Severity levels: CRITICAL, HIGH, MEDIUM, LOW"
          echo "- Scan type: File system (entire repository)"

          # Show file count for context
          echo ""
          echo "📁 Repository Overview:"
          echo "- Total files: $(find . -type f -not -path './.git/*' -not -path './node_modules/*' -not -path './venv/*' | wc -l | tr -d ' ')"
          echo "- Shell scripts: $(find . -name '*.sh' | wc -l | tr -d ' ')"
          echo "- YAML files: $(find . -name '*.yaml' -o -name '*.yml' | wc -l | tr -d ' ')"
          echo "- Terraform files: $(find . -name '*.tf' | wc -l | tr -d ' ')"

      # Step 5: Upload SARIF results to GitHub Security tab
      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@f205ea1c3313d32999d8d6a48b4f6530d4437b38 # v4.37.4
        if: always()
        continue-on-error: true  # Don't fail if GitHub Advanced Security is not enabled
        with:
          sarif_file: 'trivy-results.sarif'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Step 6: Strict enforcement confirmation
      - name: Security scan enforcement status
        if: success()
        run: |
          echo "✅ Security scan PASSED - No findings detected"
          echo "🔒 Zero-tolerance policy enforced successfully"

  # Code quality job - performs additional code quality checks
  # This job focuses on detecting common code quality issues, hardcoded secrets,
  # and ensuring proper file permissions and structure
  code-quality:
    name: Code Quality
    runs-on: ubuntu-26.04

    steps:
      # Step 1: Checkout repository code for quality analysis
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Step 2: Set up Python environment for quality checks
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # Step 3: Install Python dependencies for quality analysis
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml  # Required for YAML analysis

      - name: Lint diagrams shell scripts
        run: |
          if [ -d "diagrams" ] && ls diagrams/*.sh 1>/dev/null 2>&1; then
            echo "Running shellcheck on diagrams/ scripts..."
            shellcheck diagrams/*.sh
          else
            echo "No shell scripts found in diagrams/ — skipping"
          fi

      - name: Check for common issues
        run: |
          echo "Checking for common code quality issues..."

          # Check for hardcoded secrets in configuration files
          if grep -r "password\|secret\|key" --include="*.sh" --include="*.yaml" --include="*.tf" . | grep -v "example\|template\|test"; then
            echo "Warning: Potential hardcoded secrets found"
          fi

          # Check for TODO/FIXME comments that need attention
          if grep -r "TODO\|FIXME" --include="*.sh" --include="*.yaml" --include="*.tf" --include="*.md" .; then
            echo "Info: TODO/FIXME comments found"
          fi

          # Check for proper file permissions on critical scripts
          if [ -x "scripts/deploy.sh" ]; then
            echo "Info: deploy.sh is executable"
          else
            echo "Warning: deploy.sh should be executable"
          fi

  # Warp project CI/CD job - Python package testing and validation
  warp-ci:
    name: Warp CI/CD
    runs-on: ubuntu-26.04
    strategy:
      matrix:
        python-version: ['3.10', '3.14.7']
    if: |
      github.event_name != 'workflow_dispatch' ||
      github.event.inputs.test_suite == 'all' ||
      github.event.inputs.test_suite == 'warp'

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install yq
        run: |
          chmod +x scripts/ci/install-yq.sh
          ./scripts/ci/install-yq.sh

      - name: Test pinned versions compatibility
        run: ./scripts/test-warp-pinned-versions.sh

      - name: Run Warp Python CI
        run: ./scripts/ci/run-python-ci.sh warp

      - name: Upload coverage reports
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: warp-coverage-report-${{ matrix.python-version }}
          path: warp/htmlcov/
          retention-days: 7
          if-no-files-found: ignore

      - name: Run Trivy security scan (Zero-Tolerance)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        if: matrix.python-version == env.PYTHON_VERSION
        with:
          version: 'v${{ env.TRIVY_VERSION }}'
          scan-type: 'fs'
          scan-ref: 'warp'
          format: 'table'
          output: 'trivy-warp-results.txt'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
          scanners: 'vuln,secret,misconfig'
          exit-code: '1'
          trivyignores: '.trivyignore'

      - name: Display Trivy results
        if: always() && matrix.python-version == env.PYTHON_VERSION
        run: |
          echo "Trivy Security Scan Results for Warp (ZERO-TOLERANCE):"
          echo "=========================================================="
          if [ -f "trivy-warp-results.txt" ]; then
            cat trivy-warp-results.txt
          else
            echo "No vulnerabilities found - scan passed"
          fi
          echo "=========================================================="

  # OpenEMR DR Python CI — restore/backup orchestrator with strict quality gates
  openemr-dr-ci:
    name: OpenEMR DR Python CI
    runs-on: ubuntu-26.04
    if: |
      github.event_name != 'workflow_dispatch' ||
      github.event.inputs.test_suite == 'all' ||
      github.event.inputs.test_suite == 'openemr_dr'

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install yq
        run: |
          chmod +x scripts/ci/install-yq.sh
          ./scripts/ci/install-yq.sh

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Test pinned versions compatibility
        run: ./scripts/test-openemr-dr-pinned-versions.sh

      - name: Run OpenEMR DR Python CI
        run: ./scripts/ci/run-python-ci.sh openemr_dr

      - name: Upload coverage reports
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: openemr-dr-coverage-report
          path: scripts/openemr_dr/htmlcov/
          retention-days: 7
          if-no-files-found: ignore

  # Credential Rotation CI job
  credential-rotation-ci:
    name: Credential Rotation CI
    runs-on: ubuntu-26.04
    if: |
      github.event_name != 'workflow_dispatch' ||
      github.event.inputs.test_suite == 'all' ||
      github.event.inputs.test_suite == 'credential_rotation'

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install yq
        run: |
          chmod +x scripts/ci/install-yq.sh
          ./scripts/ci/install-yq.sh

      - name: Test pinned versions compatibility
        run: ./scripts/test-credential-rotation-pinned-versions.sh

      - name: Run Credential Rotation Python CI
        run: ./scripts/ci/run-python-ci.sh credential_rotation

      - name: Upload coverage reports
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: credential-rotation-coverage-report
          path: tools/credential-rotation/htmlcov/
          retention-days: 7
          if-no-files-found: ignore

      - name: Run Trivy security scan (Zero-Tolerance)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          version: 'v${{ env.TRIVY_VERSION }}'
          scan-type: 'fs'
          scan-ref: 'tools/credential-rotation'
          format: 'table'
          output: 'trivy-credential-rotation-results.txt'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
          scanners: 'vuln,secret,misconfig'
          exit-code: '1'
          trivyignores: '.trivyignore'

      - name: Display Trivy results
        if: always()
        run: |
          echo "Trivy Security Scan Results for Credential Rotation (ZERO-TOLERANCE):"
          echo "====================================================================="
          if [ -f "trivy-credential-rotation-results.txt" ]; then
            cat trivy-credential-rotation-results.txt
          else
            echo "No vulnerabilities found - scan passed"
          fi
          echo "====================================================================="

  # Local read-only codebase knowledge MCP package
  knowledge-mcp-ci:
    name: Knowledge MCP Python CI
    runs-on: ubuntu-26.04
    if: |
      github.event_name != 'workflow_dispatch' ||
      github.event.inputs.test_suite == 'all' ||
      github.event.inputs.test_suite == 'knowledge_mcp'

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install pinned uv
        run: python -m pip install "uv==${UV_VERSION}"

      - name: Sync locked development environment
        run: uv sync --project tools/codebase-mcp --frozen --extra dev

      - name: Run quality and security checks
        run: |
          uv run --project tools/codebase-mcp ruff check \
            --config tools/codebase-mcp/pyproject.toml tools/codebase-mcp
          uv run --project tools/codebase-mcp ruff format --check \
            --config tools/codebase-mcp/pyproject.toml tools/codebase-mcp
          uv run --project tools/codebase-mcp mypy \
            --config-file tools/codebase-mcp/pyproject.toml tools/codebase-mcp/src
          uv run --project tools/codebase-mcp bandit \
            -c tools/codebase-mcp/pyproject.toml -q -r tools/codebase-mcp/src

      - name: Run offline-capable MCP tests
        run: uv run --project tools/codebase-mcp pytest tools/codebase-mcp/tests

      - name: Build and verify uvx entry point
        run: |
          uv build --project tools/codebase-mcp
          uvx --from ./tools/codebase-mcp \
            openemr-eks-mcp --repo-root . --check
          uv run --project tools/codebase-mcp --frozen --offline \
            openemr-eks-mcp --repo-root . --check

  # Floci integration suites — live AWS API tests against the Floci emulator
  floci-integration:
    name: Floci Integration
    runs-on: ubuntu-26.04
    if: |
      github.event_name != 'workflow_dispatch' ||
      github.event.inputs.test_suite == 'all' ||
      github.event.inputs.test_suite == 'floci'
    env:
      AWS_ENDPOINT_URL: http://localhost:4566
      AWS_DEFAULT_REGION: us-east-1
      AWS_ACCESS_KEY_ID: test
      AWS_SECRET_ACCESS_KEY: test
      AWS_EC2_METADATA_DISABLED: 'true'
      AWS_CONFIG_FILE: /dev/null
      AWS_SHARED_CREDENTIALS_FILE: /dev/null

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install yq
        run: |
          chmod +x scripts/ci/install-yq.sh
          ./scripts/ci/install-yq.sh

      - name: Resolve Floci image pin from versions.yaml
        run: |
          FLOCI_VERSION="$(yq eval '.applications.floci.current' versions.yaml)"
          echo "FLOCI_VERSION=${FLOCI_VERSION}" >> "$GITHUB_ENV"
          echo "Using floci/floci:${FLOCI_VERSION}"

      - name: Install AWS CLI and BATS
        run: |
          if ! command -v aws >/dev/null 2>&1; then
            curl -fsSL "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o /tmp/awscliv2.zip
            unzip -q /tmp/awscliv2.zip -d /tmp
            sudo /tmp/aws/install
          fi
          sudo apt-get update
          sudo apt-get install -y bats jq

      - name: Start Floci
        run: |
          chmod +x tests/floci/*.sh scripts/run-floci-integration.sh
          docker compose -f tests/floci/compose.yaml up -d
          ./tests/floci/wait.sh

      - name: Install Python package test deps
        run: |
          ./scripts/install-python-dev.sh warp
          ./scripts/install-python-dev.sh credential_rotation
          ./scripts/install-python-dev.sh openemr_dr

      - name: Run Floci integration suites
        run: ./scripts/run-floci-integration.sh

      - name: Stop Floci
        if: always()
        run: docker compose -f tests/floci/compose.yaml down || true

  # Floci e2e-lite — CI-mocked backup/restore DR scenario (not the real-AWS gate)
  floci-e2e-lite:
    name: Floci E2E Lite
    runs-on: ubuntu-26.04
    if: |
      github.event_name != 'workflow_dispatch' ||
      github.event.inputs.test_suite == 'all' ||
      github.event.inputs.test_suite == 'floci' ||
      github.event.inputs.test_suite == 'floci_e2e_lite'
    env:
      AWS_ENDPOINT_URL: http://localhost:4566
      AWS_DEFAULT_REGION: us-east-1
      AWS_ACCESS_KEY_ID: test
      AWS_SECRET_ACCESS_KEY: test
      AWS_EC2_METADATA_DISABLED: 'true'
      AWS_CONFIG_FILE: /dev/null
      AWS_SHARED_CREDENTIALS_FILE: /dev/null

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install yq
        run: |
          chmod +x scripts/ci/install-yq.sh
          ./scripts/ci/install-yq.sh

      - name: Resolve Floci image pin from versions.yaml
        run: |
          FLOCI_VERSION="$(yq eval '.applications.floci.current' versions.yaml)"
          echo "FLOCI_VERSION=${FLOCI_VERSION}" >> "$GITHUB_ENV"
          echo "Using floci/floci:${FLOCI_VERSION}"

      - name: Install AWS CLI
        run: |
          if ! command -v aws >/dev/null 2>&1; then
            curl -fsSL "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o /tmp/awscliv2.zip
            unzip -q /tmp/awscliv2.zip -d /tmp
            sudo /tmp/aws/install
          fi
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Start Floci
        run: |
          chmod +x tests/floci/*.sh scripts/test-floci-e2e-lite.sh
          docker compose -f tests/floci/compose.yaml up -d
          ./tests/floci/wait.sh

      - name: Run Floci e2e-lite scenario
        run: ./scripts/test-floci-e2e-lite.sh

      - name: Stop Floci
        if: always()
        run: docker compose -f tests/floci/compose.yaml down || true

  # Summary job - generates comprehensive test results summary
  # This job collects results from all previous jobs and provides a unified
  # overview of the entire CI/CD pipeline execution
  summary:
    name: Test Summary
    runs-on: ubuntu-26.04
    needs:
      - test
      - lint-and-validate
      - security-scan
      - code-quality
      - python-requirements-validate
      - warp-ci
      - openemr-dr-ci
      - credential-rotation-ci
      - knowledge-mcp-ci
      - floci-integration
      - floci-e2e-lite
    if: always()

    steps:
      # Step 1: Checkout repository code for summary generation
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Step 2: Download all test result artifacts from previous jobs
      - name: Download all test results
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: test-results/

      # Step 3: Generate comprehensive summary report
      # This step creates a unified view of all test results and job outcomes
      - name: Generate summary report
        env:
          EVENT_NAME: ${{ github.event_name }}
        run: |
          echo "## 🧪 CI/CD Test Results Summary"
          echo ""

          # Show test results summary
          echo "### 📊 Test Results Overview"
          echo ""

          for result_file in test-results/*/test-report-*.txt; do
            if [ -f "$result_file" ]; then
              suite_name=$(basename "$(dirname "$result_file")")
              echo "**$suite_name**:"
              # Show just the summary line from the report
              if grep -q "Test Results Summary" "$result_file"; then
                grep "Test Results Summary" "$result_file"
              else
                echo "  Report available: $(basename "$result_file")"
              fi
              echo ""
            fi
          done

          echo "## 🚀 Warp CI/CD Status"
          if [ "${{ needs.warp-ci.result }}" == "success" ]; then
            echo "✅ **Warp tests passed successfully!**"
          elif [ "${{ needs.warp-ci.result }}" == "skipped" ]; then
            echo "⏭️ **Warp tests skipped** (not selected for this manual run)"
          else
            echo "❌ **Warp tests failed or had issues** - Check the warp-ci job logs"
          fi
          echo ""

          echo "## 🔄 OpenEMR DR Python CI Status"
          if [ "${{ needs.openemr-dr-ci.result }}" == "success" ]; then
            echo "✅ **OpenEMR DR Python tests passed successfully!**"
          elif [ "${{ needs.openemr-dr-ci.result }}" == "skipped" ]; then
            echo "⏭️ **OpenEMR DR tests skipped** (not selected for this manual run)"
          else
            echo "❌ **OpenEMR DR tests failed** - Check the openemr-dr-ci job logs"
          fi
          echo ""

          echo "## 🔐 Credential Rotation CI Status"
          if [ "${{ needs.credential-rotation-ci.result }}" == "success" ]; then
            echo "✅ **Credential rotation tests passed successfully!**"
          elif [ "${{ needs.credential-rotation-ci.result }}" == "skipped" ]; then
            echo "⏭️ **Credential rotation tests skipped** (not selected for this manual run)"
          else
            echo "❌ **Credential rotation tests failed** - Check the credential-rotation-ci job logs"
          fi
          echo ""

          echo "## 🧠 Knowledge MCP Python CI Status"
          if [ "${{ needs.knowledge-mcp-ci.result }}" == "success" ]; then
            echo "✅ **Knowledge MCP tests and uvx verification passed successfully!**"
          elif [ "${{ needs.knowledge-mcp-ci.result }}" == "skipped" ]; then
            echo "⏭️ **Knowledge MCP tests skipped** (not selected for this manual run)"
          else
            echo "❌ **Knowledge MCP tests failed** - Check the knowledge-mcp-ci job logs"
          fi
          echo ""

          echo "## ☁️ Floci Integration Status"
          if [ "${{ needs.floci-integration.result }}" == "success" ]; then
            echo "✅ **Floci integration suites passed successfully!**"
          elif [ "${{ needs.floci-integration.result }}" == "skipped" ]; then
            echo "⏭️ **Floci integration skipped** (not selected for this manual run)"
          else
            echo "❌ **Floci integration failed** - Check the floci-integration job logs"
          fi
          echo ""

          echo "## ☁️ Floci E2E Lite Status"
          if [ "${{ needs.floci-e2e-lite.result }}" == "success" ]; then
            echo "✅ **Floci e2e-lite scenario passed successfully!**"
          elif [ "${{ needs.floci-e2e-lite.result }}" == "skipped" ]; then
            echo "⏭️ **Floci e2e-lite skipped** (not selected for this manual run)"
          else
            echo "❌ **Floci e2e-lite failed** - Check the floci-e2e-lite job logs"
          fi
          echo ""

          echo "## 📦 Python Requirements Validation"
          if [ "${{ needs.python-requirements-validate.result }}" == "success" ]; then
            echo "✅ **All Python requirement pins match versions.yaml**"
          elif [ "${{ needs.python-requirements-validate.result }}" == "skipped" ]; then
            echo "⏭️ **Requirements validation skipped** (not selected for this manual run)"
          else
            echo "❌ **Requirements pin validation failed** - Check python-requirements-validate job logs"
          fi
          echo ""

          echo "## 📊 Overall Status"
          OVERALL_OK=true
          ALLOW_SKIP=false
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            ALLOW_SKIP=true
          fi
          for job_result in \
            "${{ needs.test.result }}" \
            "${{ needs.lint-and-validate.result }}" \
            "${{ needs.security-scan.result }}" \
            "${{ needs.code-quality.result }}" \
            "${{ needs.python-requirements-validate.result }}" \
            "${{ needs.warp-ci.result }}" \
            "${{ needs.openemr-dr-ci.result }}" \
            "${{ needs.credential-rotation-ci.result }}" \
            "${{ needs.knowledge-mcp-ci.result }}" \
            "${{ needs.floci-integration.result }}" \
            "${{ needs.floci-e2e-lite.result }}"; do
            if [ "$job_result" = "success" ]; then
              continue
            fi
            if [ "$ALLOW_SKIP" = true ] && [ "$job_result" = "skipped" ]; then
              continue
            fi
            OVERALL_OK=false
            break
          done
          if [ "$OVERALL_OK" = true ]; then
            if [ "$ALLOW_SKIP" = true ]; then
              echo "✅ **All required CI jobs passed or were skipped appropriately!**"
            else
              echo "✅ **All required CI jobs passed!**"
            fi
          else
            echo "❌ **Some CI jobs failed. Please review the logs above.**"
          fi

          echo ""
          echo "## 🔒 Security Scan Status (Zero-Tolerance Policy)"
          if [ "${{ needs.security-scan.result }}" == "success" ]; then
            echo "✅ **Security scan PASSED - No findings detected**"
            echo "- Zero-tolerance policy enforced: All severity levels checked (CRITICAL, HIGH, MEDIUM, LOW)"
            echo "- Scanners: Vulnerability, Secret Detection, Configuration"
            echo "- If GitHub Advanced Security is enabled, results are also available in the Security tab"
          elif [ "${{ needs.security-scan.result }}" == "skipped" ]; then
            echo "⏭️ **Security scan skipped** (not selected for this manual run)"
          else
            echo "❌ **Security scan FAILED - Findings detected**"
            echo "- Check the security-scan job logs for detailed vulnerability results"
            echo "- ALL findings must be remediated before merging (zero-tolerance policy)"
          fi

          if [ "$OVERALL_OK" != true ]; then
            exit 1
          fi
